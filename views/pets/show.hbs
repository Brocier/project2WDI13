<div class="nav">
  <a href="/users/{{userId}}">Back to User</a>
  <a href="/users/{{userId}}/pets">Back to Pets</a>
  <a href="/users/{{userId}}/pets/{{petId}}/delete">Remove this pet</a>
  <a href="/users/{{userId}}/pets/{{petId}}/edit">Edit this pet's details</a> Can't get editting nested things to work
</div>

<div class="pet-info">
  <h1>{{pet.name}}</h1>
  <h2>Age: {{pet.age}}</h2>
</div>
</br>
<div>
  <h3>Pictures of {{pet.name}}:</h3>
</div>
<div>
  {{#each pet.images}}
  <div>
    <a href="/users/{{../userId}}/pets/{{../petId}}/images/{{this._id}}">
      <img src="{{this.imageUrl}}" alt="{{this.description}}" height="200"> </a>
  </div>
  <div>
    {{this.description}}
  </div>
  {{/each}}
  <a href="/users/{{userId}}/pets/{{petId}}/images/new">Add a Pic of {{pet.name}}</a>
</div>